#include <stdio.h> 
// we use <> for starndard libreries
#include "funcions.h" // we use "" to select .h files in the same directory
// #include "funcions.c"



void main() {
	float radi;
	radi = 10;
	//area = PI*(radi*radi)
	printf("%f\n", area(radi) );

}
